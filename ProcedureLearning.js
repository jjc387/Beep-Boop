function click_here()
    /*
     *When clicked, it makes the next box over transparent,
     *changes the position of the "Click here" button to the next box,
     *and changes the text for the step listed.
     */

function change_text()
    /*
     *Changes the text on the top to the next step
     */
    
function drag_and_drop()
    /*
     *Moves an image into an empty box
     */
    
function check_answer()
    /*Returns: True if image placed in correct box. False if not.
     */

function stop()
    /*
     *Does not allow a user to place an incorrect image in a box.
     */
    
function completed()
    /*
     *A "Mission Accomplished" screen pops up
     */
    
function finish()
    /*
     *A button on the "Mission Accomplished" screen to return to game page
     */
    
    

